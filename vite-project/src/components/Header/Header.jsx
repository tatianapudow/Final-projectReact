export default function Header(){
    return(
        <header>
            <div>
                <nav>
                    <NavLink to="/">Главная</NavLink>
                </nav>

            </div>
        </header>
    );
}